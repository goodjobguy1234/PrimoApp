package com.example.primohomepage.data

import javax.inject.Inject

class PrimoFeedLocalDatasource @Inject constructor(
) {
    //todo setup dao room
    // setup dao here
    // dao -> update remote -> get (single source of truth)
    // make dao return flow!!
}